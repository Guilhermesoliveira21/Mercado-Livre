import { FiLoader } from "react-icons/fi";
import './loader.css'

const Loader = () => {
        return(
        <div className="loadd">
                < FiLoader className="loader"/>
        </div>
        )
}

export default Loader;